@model KP_APP.Models.sost_departments

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>sost_departments</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="id_sost_dep" />
            <div class="form-group">
                <label asp-for="id_dep" class="control-label"></label>
                <select asp-for="id_dep" class="form-control" asp-items="ViewBag.id_dep"></select>
                <span asp-validation-for="id_dep" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="id_pos" class="control-label"></label>
                <select asp-for="id_pos" class="form-control" asp-items="ViewBag.id_pos"></select>
                <span asp-validation-for="id_pos" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
