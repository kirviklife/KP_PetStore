@model IEnumerable<KP_APP.Models.tov_kategories>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.kategor_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tov_kategories1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.kategor_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tov_kategories1.kategor_name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_kategor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_kategor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_kategor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
